original claude prompt was lost. 

Secondary claude code prompts
- this is a PTZ control system for a two axis PT gimbal. both of the nema 17 steppers have mks servo42c drivers on them, with step,dir, en, com pins exposed to an esp32 wroom MCU. esp32 should communicate with an xbox one s controller directly via bluetooth. 

take advantage of the mks drivers hall effect sensors, for homing. project should have full GNU style CLI commands for debugging, and setup, as well as a help section, and a dedicated user manual that is written in human facing language.

Project Goals:
1. full control over pan and tilt steppers
2. communication between mks servo42C drivers
3. Xbox gamepad communication
4. Full GNU style CLI structure
5. User manual
6. extensibility - easy addition of peripherals.  



Constants:
1. MKS Servo42c drivers
2. Gear Ratios -  pan axis has a gear ratio of 144:17 and the tilt has a ratio of 64:21
3.



---PHASE 2 Application Goals---

1. Addition of a raspberry pi 4 with an IR pI camera (ribbon cable version) to the end effector of the gimbal
2. extensible web application (or native) for viewing camera feed in real time with an actual usable framerate. 
3. control of the gimbal through the application while viewing video feed. 
4. maintain segregation of the esp32/pi keeping gimbal control and communication with stepper drivers on the esp as lon as...
   - lag isnt too noticable
   - overhead communication isnt obscene
   - there is not an obvious better path forward using the pi as the all in one controll module
   - communication protocol between pi and esp is not buggy
5. Implementation of simple object detection via the camera
6. implement a simple API to track an certain object via direct selection of that object or
   - proramatic detection and tracking of objects
   - face detection and tracking

Wish-list 
   - Maintain ability to control via xbox controller if feasable via selection on the application
   - easy graphical interface for all controls, settings, param changes via application
   - Claude Integration for object detection/face detection of local people that have been willfully saved into local database
   - Keep as much of the work local, only using APIs when necessary to cut down on token use, and network reliance.

---End PHASE2 Goals---

## Session History & Continuation Guide
*Updated: 2026-03-22 (session 3 — end of day)*

---

### PICK UP HERE TOMORROW

**The immediate next task is switching from UART drive mode to Step/Dir/EN (stepper) mode.**
This is the decision we landed on at end of session because UART velocity control is unreliable
for real-time stop/start from the web UI (MKS driver holds last velocity indefinitely).

**Stepper mode is already fully implemented in the firmware** — just needs the MKS driver
reconfigured and the Step/Dir/EN pins wired up. Three steps:

1. **On each MKS driver OLED:** Change Work Mode → `CR_vFOC` (was `CR_UART`). Leave all else the same.

2. **Wire 3 extra pins per driver** (UART wires stay connected for homing/encoder):
   | Driver | STEP pin | DIR pin | EN pin |
   |--------|----------|---------|--------|
   | Pan    | GPIO 25  | GPIO 26 | GPIO 27 |
   | Tilt   | GPIO 32  | GPIO 33 | GPIO 19 |

3. **In PlatformIO:** select environment `esp32dev_stepper` (bottom bar in VS Code) and Upload.

4. **Test via serial monitor** at 115200:
   - `vel pan 30` → should move
   - `stop all` → should stop instantly
   - If direction wrong: flip `PAN_DIR_INVERT` or `TILT_DIR_INVERT` in `include/config.h` and reflash

5. **On Pi:** `git pull`, install python-dotenv (`pip install python-dotenv`), restart server.
   Then set inversion in `pi/.env` if needed:
   ```
   PAN_INVERT=true
   TILT_INVERT=true
   ```

---

### Current Hardware Status (as of 2026-03-22 end of session)

- **ESP32:** Firmware written and on GitHub. **Partially tested** — was running in CR_UART mode.
  - UART mode had unreliable stop behavior (MKS holds last velocity) — switching to stepper mode.
  - `vel` CLI command added this session and flashed (user confirmed).
  - `PAN_DIR_INVERT` / `TILT_DIR_INVERT` flags added to config.h for easy direction fix.
  - **Next flash must use `env:esp32dev_stepper`** after changing MKS work mode to CR_vFOC.

- **Pi 4:** Running, server working, video streaming confirmed in browser.
  - Hostname: `CIWS` (ssh pi@CIWS.local)
  - Camera: **IMX219** (Pi Camera Module 2 / NoIR v2) — confirmed working
  - Camera rotation: `CAMERA_ROTATION=2` in config.py (camera is physically inverted — 180°)
  - `python-dotenv` added to requirements.txt — run `pip install python-dotenv` on Pi after `git pull`
  - `.env` file does NOT exist on the Pi yet — create it at `~/PTZ_controller/pi/.env`
  - Git is installed and repo is cloned at `~/PTZ_controller`
  - Server launch: `cd ~/PTZ_controller/pi && source venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000`

- **Web UI:** Working. Video streams. Controls partially working but:
  - Stop on release was broken in UART mode — will be fixed by stepper mode switch
  - Joystick now has squared response curve (feels better at low speed)
  - Default speed lowered to 45°/s (was 180°/s)
  - Axis inversion via `.env` file (PAN_INVERT / TILT_INVERT) — `.env` now actually loads via dotenv

- **Gimbal mechanics:** Not yet assembled with Pi on end effector. Camera ribbon routing TBD.

- **YOLO/ultralytics:** `Illegal instruction` crash on Pi 4 — torch build incompatible with ARM Cortex-A72.
  Skip for now. Face detection (MediaPipe) works fine and is the primary tracking use case.

---

### What Has Been Built

**ESP32 Firmware — all written, on GitHub, stepper mode ready to flash:**
- `src/drivers/MKSServo42C.h/.cpp` — UART driver (motion, read-back, homing, raw diag)
- `src/motion/MotionController.h/.cpp` — dual-mode (UART + stepper), velocity, homing, NVS
- `src/input/GamepadInput.h/.cpp` — Bluepad32 Xbox One S
- `src/cli/CLI.h/.cpp` — GNU CLI including new `vel` command
- `include/config.h` — pins, gear ratios, `PAN_DIR_INVERT`, `TILT_DIR_INVERT`

**Pi Application (`pi/` directory) — all written, on GitHub, running on Pi:**

| File/Dir | Status | What it does |
|---|---|---|
| `app/config.py` | ✅ | All constants. dotenv loading. Camera FoV IMX219: 47°H × 28°V |
| `app/state.py` | ✅ | Thread-safe shared state |
| `app/serial_bridge/protocol.py` | ✅ | ESP32 CLI command builders + parsers |
| `app/serial_bridge/bridge.py` | ✅ | Priority-queue serial writer, auto-reconnect |
| `app/camera/capture.py` | ✅ | picamera2 thread, CAMERA_ROTATION support |
| `app/camera/stream.py` | ✅ | MJPEG async generator |
| `app/vision/pipeline.py` | ✅ | Vision thread + tracker wired in |
| `app/vision/detector_null.py` | ✅ | Pass-through |
| `app/vision/detector_face.py` | ✅ | MediaPipe face detection |
| `app/vision/detector_yolo.py` | ✅ | YOLOv8-nano (crashes on Pi 4 — skip for now) |
| `app/vision/tracker.py` | ✅ | PID tracking → vel commands to ESP32 |
| `app/api/router_stream.py` | ✅ | GET /stream |
| `app/api/router_control.py` | ✅ | WS /ws/control — vel/stop/estop/home/tracking |
| `app/api/router_detection.py` | ✅ | Detection mode switching |
| `app/api/router_settings.py` | ✅ | Motion settings + /api/settings/ui for frontend |
| `app/main.py` | ✅ | App factory, lifespan |
| `app/frontend/index.html` | ✅ | Dark UI: video, joystick, overlay |
| `app/frontend/js/app.js` | ✅ | WS, telemetry, buttons, fetches server config |
| `app/frontend/js/controls.js` | ✅ | Joystick (squared curve) + WASD, window-level events |
| `app/frontend/js/detection_overlay.js` | ✅ | Canvas boxes, click-to-track |
| `app/db/` | ❌ | SQLite face DB (Phase 2f — not started) |
| `app/vision/recognizer.py` | ❌ | Face recognition (Phase 2f) |
| `app/api/router_faces.py` | ❌ | Face enrollment API (Phase 2f) |
| `app/api/router_claude.py` | ❌ | Claude NL integration (Phase 2g, optional) |

**GitHub repo:** `https://github.com/blueteef/PTZ_controller` (private)

---

### What Has Been Built

**ESP32 Firmware (PlatformIO / Arduino-ESP32) — written, needs flash + hardware test:**
- `src/drivers/MKSServo42C.h/.cpp` — full UART driver class (motion, read-back, homing, raw diag)
- `src/motion/MotionController.h/.cpp` — axis abstraction, velocity control, soft limits, homing state machine, NVS settings persistence
- `src/input/GamepadInput.h/.cpp` — Bluepad32 Xbox One S integration
- `src/cli/CLI.h/.cpp` — GNU-style serial CLI (help, move, stop, home, ping, diag, reset, save, estop, status, set, **vel**)
- `include/config.h` — all pin assignments, gear ratios, motion defaults, task priorities
- `scripts/mks_setup.py` — standalone Python UART tool for direct driver config via FTDI

**NEW in session 3 — `vel` CLI command added to ESP32:**
- `vel <pan|tilt|all> <deg_s>` — continuous velocity command, no response printed
- This is the primary interface for Pi tracking loop (sent at ~20-50Hz)
- Needs to be flashed to ESP32 before Pi serial bridge will work correctly

**Pi Application (`pi/` directory) — written, pip install in progress:**

| File/Dir | Status | What it does |
|---|---|---|
| `app/config.py` | ✅ written | All constants. Camera FoV set for IMX219: 47°H × 28°V |
| `app/state.py` | ✅ written | Thread-safe shared state (frames, detections, position) |
| `app/serial_bridge/protocol.py` | ✅ written | Command builders + response parsers for ESP32 CLI |
| `app/serial_bridge/bridge.py` | ✅ written | Priority-queue serial writer, auto-reconnect, position poller |
| `app/camera/capture.py` | ✅ written | picamera2 background thread, stubs on non-Pi |
| `app/camera/stream.py` | ✅ written | MJPEG async generator for HTTP stream |
| `app/vision/pipeline.py` | ✅ written | Vision thread, swappable detectors |
| `app/vision/detector_null.py` | ✅ written | Pass-through (no detection) |
| `app/vision/detector_face.py` | ✅ written | MediaPipe face detection |
| `app/vision/detector_yolo.py` | ✅ written | YOLOv8-nano object detection |
| `app/vision/tracker.py` | ✅ written | PID tracking controller → vel commands to ESP32 |
| `app/api/router_stream.py` | ✅ written | GET /stream — MJPEG StreamingResponse |
| `app/api/router_control.py` | ✅ written | WS /ws/control — bidirectional gimbal control + telemetry |
| `app/api/router_detection.py` | ✅ written | POST/GET /api/detection — mode switching |
| `app/api/router_settings.py` | ✅ written | GET/PUT /api/settings — motion params |
| `app/main.py` | ✅ written | FastAPI app factory, lifespan, mounts all routers |
| `app/frontend/index.html` | ✅ written | Dark UI: video feed, virtual joystick, detection overlay |
| `app/frontend/style.css` | ✅ written | Dark theme styles |
| `app/frontend/js/app.js` | ✅ written | WS connection, telemetry handler, button wiring |
| `app/frontend/js/controls.js` | ✅ written | Virtual joystick + WASD keyboard → velocity commands |
| `app/frontend/js/detection_overlay.js` | ✅ written | Canvas overlay, click-to-track |
| `app/db/` | ❌ not yet | SQLite face database (Phase 2f) |
| `app/vision/recognizer.py` | ❌ not yet | Face recognition (Phase 2f) |
| `app/api/router_faces.py` | ❌ not yet | Face enrollment REST API (Phase 2f) |
| `app/api/router_claude.py` | ❌ not yet | Claude NL integration (Phase 2g, optional) |
| `tests/test_protocol.py` | ✅ written | Unit tests for protocol module (run on any machine) |
| `tests/test_tracker.py` | ✅ written | Unit tests for tracker PID math |
| `setup.sh` | ✅ written | Pi provisioning script |
| `scripts/test_serial.py` | ✅ written | Serial smoke test (run on Pi with ESP32 connected) |
| `scripts/download_models.sh` | ✅ written | Downloads YOLOv8-nano weights to models/ |

**Current gamepad mapping (Xbox One S via Bluepad32):**
- Left stick → pan/tilt velocity
- Right bumper (RB) → fine-speed mode (15% scale)
- Left bumper (LB) → latch laser on/off
- Left trigger → laser strobe ~25 Hz while held
- D-pad L/R → pan speed multiplier ±0.25 (range 0.25–10.0, default 4.25×)
- D-pad U/D → tilt speed multiplier ±0.25 (range 0.25–10.0, default 10.0×)
- A → stop all motion
- B → emergency stop (latching)
- Y → home both axes
- Menu → toggle soft limits

**Laser:** GPIO 13 → FET driver gate. Latch = steady on. Trigger = 25 Hz strobe.

**Motion defaults (config.h):**
- Max speed: 180 deg/s, Accel: 30 deg/s², Fine scale: 0.15×
- Soft limits: DISABLED (re-enable after homing configured)
- Pan: ±180°, Tilt: −45° to +90°

**NVS namespace:** `ptz_cfg` — survives flashes. After changing config.h defaults: flash → `reset` → `save` in CLI.

---

### What Still Needs Doing (in order)

**IMMEDIATE — Switch to stepper drive mode (do this first tomorrow):**
1. Change both MKS drivers to `CR_vFOC` via OLED
2. Wire Step/Dir/EN pins (see table in "PICK UP HERE" section above)
3. Flash `env:esp32dev_stepper` from PlatformIO
4. Test `vel pan 30` / `stop all` in serial monitor — stop should be instant
5. Adjust `PAN_DIR_INVERT` / `TILT_DIR_INVERT` in config.h if direction wrong, reflash

**Pi — after stepper mode confirmed working:**
1. `git pull` on Pi
2. `pip install python-dotenv` (or `pip install -r requirements.txt`)
3. Create `pi/.env` with inversion flags if needed
4. Restart server and test web UI controls — stop-on-release should now work

**Tracking — after controls are solid:**
1. Enable Face detection in dropdown
2. Confirm bounding boxes appear around faces
3. Click a box — gimbal should follow

**Remaining Phase 1 items (lower priority, do after stepper mode works):**
1. **32-byte pan UART drain** — pan driver returns extra bytes to 0x3A query (may not matter in stepper mode since UART is only used for homing/encoder now)
2. **Homing** — set `0_Mode = DirMode, 0_Speed = 2, 0_Dir = CW` on each driver OLED, then `Set 0` at home position. Test `home all` CLI command.
3. **Soft limits** — enable `SOFT_LIMITS_ENABLED true` in config.h once homing works

**Phase 2f (face recognition) — not started:**
- Requires dlib (compiles from source on Pi, ~30-60 min): `pip install dlib face-recognition`
- Then build: `app/db/database.py`, `app/db/face_store.py`, `app/vision/recognizer.py`, `app/api/router_faces.py`
- Add enrollment UI to frontend

**Phase 2g (optional polish):**
- YOLO object detection — needs alternative to ultralytics for Pi 4 ARM (tflite-runtime + MobileNet SSD, or Coral USB accelerator)
- Xbox controller via Browser Gamepad API (`app/frontend/js/gamepad.js`) — needs HTTPS
- Claude integration (`app/api/router_claude.py`) — set `ANTHROPIC_API_KEY` in `pi/.env`

---

### How to Pick This Up Quickly

**If continuing ESP32 work:**
1. Open project in VSCode with PlatformIO extension
2. Select environment `esp32dev_stepper` in PlatformIO (bottom bar)
3. `include/config.h` — all pins, gear ratios, `PAN_DIR_INVERT`, `TILT_DIR_INVERT`
4. Flash, open serial monitor at 115200, type `help`
5. `vel pan 30` → move, `stop all` → instant stop
6. `ping all` → verify UART to both drivers (still used for homing)
7. After config.h change: flash → `reset` → `save`

**If continuing Pi work:**
1. SSH to Pi: `ssh pi@CIWS.local`
2. `cd ~/PTZ_controller && git pull`
3. `cd pi && source venv/bin/activate`
4. Create `pi/.env` if it doesn't exist (copy from `.env.example`)
5. `uvicorn app.main:app --host 0.0.0.0 --port 8000`
6. Open browser: `http://CIWS.local:8000`

**Key Pi ↔ ESP32 serial protocol facts:**
- Port: `/dev/ttyUSB0`, 115200 baud
- ESP32 echoes every character + prints `ptz> ` prompt — Pi bridge discards this
- Motion: `vel pan 30.0\n` (no response printed), `stop all\n`, `estop\n`
- Query: `get position\n` → `pan  : X.XXX °\ntilt : X.XXX °\n`
- bridge.py priority queue: estop=0 (never dropped), velocity=1, queries=2
- Stop sends `vel pan 0` + `vel tilt 0` + `stop all` for belt-and-suspenders

**Camera facts:**
- Sensor: IMX219 (Pi Camera Module 2 / NoIR v2), confirmed working
- FoV at 1280×720: ~47°H × 28°V (tune empirically if tracking drifts)
- Camera physically inverted: `CAMERA_ROTATION=2` in config.py (180° rotate)
- `python3-dlib` NOT in Bookworm apt — install via pip when face recognition is needed
- YOLO crashes on Pi 4 (Illegal instruction — ARM incompatible torch build) — use MediaPipe for now

---

## PHASE 2 — Feasibility Analysis & Implementation Roadmap
*Evaluated: 2026-03-22*

### Verdict: All goals are feasible. Recommended approach below.

---

### Architecture Overview

```
[Raspberry Pi 4]  ← end effector, moves with gimbal
├── libcamera  →  camera capture (NoIR ribbon cable camera)
├── Web server (FastAPI + WebSockets)
│   ├── /stream         — MJPEG (or WebRTC) video feed
│   ├── /ws/control     — WebSocket: browser → Pi → ESP32 motion commands
│   └── /api/...        — REST: settings, detection mode, face enrollment
├── Vision pipeline (OpenCV / MediaPipe / YOLO)
│   ├── Face detection + recognition (local, offline)
│   ├── General object detection (YOLOv8-nano)
│   └── Tracking PID → pan/tilt velocity deltas
└── ESP32 bridge (pyserial over USB-UART, /dev/ttyUSB0)
    └── Wraps existing CLI protocol → no ESP32 firmware changes needed initially

[ESP32]  ← existing firmware, unchanged for Phase 2 start
└── Receives CLI commands from Pi over USB serial
    (e.g. "set vel pan 30.0\n")
```

---

### Goal-by-Goal Breakdown

**Goal 1 — RPi 4 + IR Camera on end effector**
- Camera in use: IMX219 (Pi Camera Module 2 / NoIR v2) — already confirmed working on this Pi
- FEASIBLE. Main challenges are mechanical: routing the ribbon and power cable
  through the moving gimbal. Options:
  - Slip ring (cleanest, adds complexity/cost)
  - Loose ribbon with strain relief and enough slack for range of motion (simplest)
- Power: 5V/3A to Pi via gimbal wiring. Pi 4 peaks ~3A under load with camera.

**Goal 2 — Real-time video web app**
- FEASIBLE. Best local approach for usable framerates:
  - Start: MJPEG stream via libcamera-apps or OpenCV → simple HTTP server
    - 720p @ 20-30fps is very achievable on Pi 4 on a local network
    - ~100-200ms latency, acceptable for manual PTZ control
  - Upgrade path: mediamtx (formerly rtsp-simple-server) → WebRTC in browser
    - Sub-100ms latency, better for tracking feedback
- Recommended start: MJPEG (simple), migrate to WebRTC only if latency is a problem

**Goal 3 — Gimbal control through the web app**
- FEASIBLE. WebSocket in the browser → FastAPI WS endpoint on Pi →
  pyserial → ESP32 CLI. The existing `set vel pan <n>` and `stop` CLI commands
  are all that's needed. No new ESP32 firmware required to start.
- Control latency (Pi→ESP32 over USB serial): <5ms. Network latency (browser→Pi
  on local WiFi): ~5-20ms. Total loop is well under perceptible lag.

**Goal 4 — Maintain ESP32/Pi segregation**
- RECOMMENDED. ESP32 continues to own real-time motion control and driver UART.
  Pi owns vision, web serving, and user interface. Communication via USB serial.
- The existing CLI protocol is sufficient. If a tighter binary protocol is ever
  needed (e.g., for <1ms command latency or bidirectional telemetry streaming),
  it can be added as a new ESP32 UART environment without breaking existing CLI.
- Red flags to watch: if pyserial drops frames at high control rates, or if
  USB serial adds jitter during tracking. Both are unlikely at ≤50Hz command rate.

**Goal 5 — Simple object detection**
- FEASIBLE on Pi 4.
  - Face detection: OpenCV Haar cascades or MediaPipe Face Detection
    - Haar: ~30fps at 640x480 on Pi 4
    - MediaPipe: ~25fps, more accurate
  - General objects: YOLOv8-nano (Ultralytics, INT8 quantized)
    - ~10-15fps at 640x480 on Pi 4 CPU (no GPU)
    - Sufficient for tracking slow/medium-speed subjects
  - Faster option if needed: Hailo-8L M.2 HAT+ for Pi 5 (not Pi 4 compatible)
    or Coral USB Accelerator (works on Pi 4, adds ~30fps headroom)

**Goal 6 — Object tracking API**
- FEASIBLE. Implementation path:
  1. Detect object/face centroid in frame (pixel x,y)
  2. Compute angular error: (centroid - frame_center) → pan/tilt delta in degrees
     using camera FoV (IMX219 at 1280×720: ~47°H × 28°V — confirmed sensor, set in config.py)
  3. PID controller on the error → velocity command to ESP32
  4. Clamp velocity to motion limits
- Face recognition with local database:
  - `face_recognition` Python library (dlib-based, offline, runs on Pi 4)
  - Enrollment: capture N frames per person, compute face encodings, store in SQLite
  - Recognition: compare live encodings to DB, threshold match → identity label
  - No API calls needed for recognition itself; this is fully local

---

### Wishlist Feasibility

**Xbox controller via app** — Feasible. Browser Gamepad API can read the controller
  and send inputs over WebSocket to the Pi. Requires HTTPS or localhost for Gamepad
  API to work in Chrome. Alternatively, connect controller to Pi via Bluetooth and
  read it in Python (pygame, evdev).

**Graphical settings/control interface** — Standard web frontend work (React/Vue or
  plain HTML). FastAPI serves the page and REST endpoints. Nothing exotic.

**Claude integration for local face recognition** — The face_recognition library
  handles the offline recognition. Claude API (claude-haiku-4-5 for cost) is useful
  for higher-level tasks: describing a scene, answering questions about detected
  people, or natural language command parsing ("track the person in the red shirt").
  Keep recognition local, use Claude only for understanding/reasoning layers.
  Use claude-haiku-4-5-20251001 as the model for lowest latency/cost.

**Keep processing local** — Achievable. All detection and tracking runs on Pi.
  Claude API is the only optional network dependency and can be feature-flagged off.

---

### Recommended Implementation Order

**Phase 2a — Hardware & Connectivity**
- [ ] Mount Pi 4 on end effector with appropriate cable routing
- [ ] Camera module connected, test `libcamera-hello` locally
- [ ] Pi powered from gimbal wiring, boots headless, accessible via SSH on WiFi
- [ ] Pi connected to ESP32 via USB, verify `ping all` works from Pi's pyserial

**Phase 2b — Video Streaming**
- [ ] Minimal FastAPI server on Pi streams MJPEG from camera
- [ ] Browser opens stream, confirms real-time video at target framerate
- [ ] Basic HTML page with video element

**Phase 2c — Web Control**
- [ ] WebSocket endpoint on Pi bridges to ESP32 CLI via pyserial
- [ ] Browser joystick/button UI sends velocity commands (pan/tilt)
- [ ] Test: control gimbal while watching live video in browser
- [ ] Add stop, home, and e-stop buttons

**Phase 2d — Object Detection**
- [ ] Integrate MediaPipe face detection into video pipeline
- [ ] Overlay bounding boxes on stream
- [ ] Add YOLOv8-nano for general object detection (toggle between modes)

**Phase 2e — Tracking**
- [ ] Implement tracking PID (start with proportional-only, tune from there)
- [ ] Click-to-track: user clicks face/object in stream, system locks on
- [ ] Automatic face tracking mode: follow first detected face
- [ ] Add lock-on indicator in UI

**Phase 2f — Face Recognition**
- [ ] Enroll faces via UI (capture, label, store encoding in SQLite)
- [ ] Live recognition overlay on stream
- [ ] "Track [name]" mode: automatically follow a specific enrolled person

**Phase 2g — Optional / Polish**
- [ ] Xbox controller via Browser Gamepad API
- [ ] Settings page in UI (motion limits, speed, detection thresholds)
- [ ] Claude integration for scene description / natural language commands

---

### Key Risks

1. **Ribbon cable mechanical failure** — The FPC ribbon is fragile over repeated
   flexing. Use a CSI extension cable designed for flex, or a slip ring.
2. **Pi power over moving gimbal wiring** — Voltage drop across long/thin wires;
   use 18AWG minimum, add local 5V regulator on Pi side if needed.
3. **Tracking latency loop** — Detection → PID → serial → driver → motion adds
   ~100-300ms. This limits tracking speed. Start with slow subjects; tune PID tight.
4. **libcamera API changes** — Pi camera stack has changed frequently. Pin the OS
   version (Raspberry Pi OS Bookworm, 2024-11-19 or later) and test before building
   the vision pipeline on top.
5. **USB serial reliability** — Pi USB can reset on power fluctuation. Use a powered
   USB hub or add a TVS diode on VBUS. Implement reconnect logic in the pyserial bridge.

---

### Technology Stack Summary

| Layer              | Technology                              |
|--------------------|------------------------------------------|
| OS                 | Raspberry Pi OS Bookworm (64-bit, lite) |
| Camera capture     | libcamera / picamera2                   |
| Video stream       | MJPEG (start) → mediamtx/WebRTC (later)|
| Web server         | FastAPI + uvicorn                        |
| ESP32 bridge       | pyserial                                 |
| Face detection     | MediaPipe or OpenCV Haar                 |
| Object detection   | YOLOv8-nano (Ultralytics)               |
| Face recognition   | face_recognition (dlib, offline)         |
| Face DB            | SQLite                                   |
| Frontend           | Plain HTML/JS or lightweight React       |
| Claude integration | claude-haiku-4-5-20251001 (optional)    |

